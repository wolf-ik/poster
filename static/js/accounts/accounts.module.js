(function () {
  'use strict';

  angular
    .module('poster.accounts', [
      'poster.accounts.controllers',
      'poster.accounts.services'
    ]);

  angular
    .module('poster.accounts.controllers', []);

  angular
    .module('poster.accounts.services', []);
})();
